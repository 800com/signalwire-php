name: Build and Test CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  tests:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        php-versions: ["8.3"]

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-versions }}

      - name: Get composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Install dependencies
        run: composer install --prefer-source --no-interaction

      - name: Verify patches applied
        run: |
          echo "Checking if Fax classes exist after patches..."
          ls -la vendor/twilio/sdk/src/Twilio/Rest/Fax/ || echo "Fax directory not found"
          if [ -f "vendor/twilio/sdk/src/Twilio/Rest/Fax/V1/FaxList.php" ]; then
            echo "FaxList.php exists, checking syntax..."
            php -l vendor/twilio/sdk/src/Twilio/Rest/Fax/V1/FaxList.php
          else
            echo "FaxList.php not found, re-applying patches..."
            composer install --prefer-source --no-interaction
          fi

      - name: Run setup tests
        run: |
          chmod +x test-setup.sh
          ./test-setup.sh

      - name: Run PHPUnit tests
        run: vendor/bin/phpunit tests

      - name: Run 800com integration tests
        run: php test-800com-integration.php
